---
parameters:
- name: "build_dir"
constants:
- name: "project_name"
  value: "MatterLightSwitchOverThread_brd4186c_v442"
steps:
- task: "copy"
  output: "./compile_commands.json"
  export: "json"
  input: "{{build_dir}}/compile_commands.json"
- task: "usage"
  input: "{{build_dir}}/{{project_name}}.out"
  map: "{{build_dir}}/{{project_name}}.map"
